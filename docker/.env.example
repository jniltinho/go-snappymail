# go-snappymail lab — copy to .env and adjust if needed
MAIL_DOMAIN=test.local
MAIL_FQDN=mail.test.local
MAIL_USER=user@test.local
MAIL_PASS=Password1@
ADMIN_EMAIL=admin@test.local
ADMIN_PASS=Password1@
SNAPPY_ADMIN_PASS=Admin1@lab

DB_NAME=postfix
DB_USER=postfix
DB_PASS=postfixPassword
MARIADB_ROOT_PASS=rootpassword

# App secrets (lab defaults — change in production)
SERVER_SECRET_KEY=change-this-secret-key-32-chars!!
SESSION_SECRET=change-me-lab-session-secret-32chars-min

VMAIL_UID=1001
VMAIL_GID=1001

GO_POSTFIXADMIN_IMAGE=jniltinho/go-postfixadmin:latest
GO_CUBEMAIL_VERSION=0.0.25
SNAPPYMAIL_IMAGE=djmaze/snappymail:latest
